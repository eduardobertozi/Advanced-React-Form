/** 
 * Estas devem ser as chaves do objeto para a gravação no BD.
 * Estes nomes vem da propriedade "name dos inputs"
*/
export type LateralTypes = {
  nomTipo: string
  desLink: string
  desIdYoutube: string
  numOrdem: number
}